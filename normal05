'''
# Задача-1:
# Напишите небольшую консольную утилиту,
# позволяющую работать с папками текущей директории.
# Утилита должна иметь меню выбора действия, в котором будут пункты:
# 1. Перейти в папку
# 2. Просмотреть содержимое текущей папки
# 3. Удалить папку
# 4. Создать папку
# При выборе пунктов 1, 3, 4 программа запрашивает название папки
# и выводит результат действия: "Успешно создано/удалено/перешел",
# "Невозможно создать/удалить/перейти"

# Для решения данной задачи используйте алгоритмы из задания easy,
# оформленные в виде соответствующих функций,
# и импортированные в данный файл из easy.py
'''


import os
from easy05 import show_all_dir

while True:
    print('''
    [1] - Go to directory
    [2] - Display current directory contents
    [3] - Remove directory
    [4] - Create directiory
    ''')
    choice = int(input('Choose an option: \n'))
    if choice == 1:
        new_dir_name = input('Specify directory name: \n')
        try:
            os.chdir(new_dir_name)
            print('You are in {} directory'.format(new_dir_name))
        except Exception:
            print('Could not enter enter {} directory'.format(new_dir_name))
    elif choice == 2:
        show_all_dir()
    elif choice == 3:
        dir_to_remove = input('Specify directory name you want to remove: \n')
        try:
            os.removedirs(dir_to_remove)
            print('Directory {} has been removed'.format(dir_to_remove))
        except Exception:
            print('Directory {} could not be removed'.format(dir_to_remove))
    elif choice == 4:
        dir_to_create = input('Specify directory name you want to create: \n')
        try:
            os.makedirs(dir_name)
            print('Directory {} has been created'.format(dir_to_remove))
        except Exception:
            print('Directory {} could not be created'.format(dir_to_remove))
    else:
        print('Your input is invalid.')
        break

